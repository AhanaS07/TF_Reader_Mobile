/**
 * There is no Expo dev server here, so `config.ts` finds no host to derive and
 * falls back to EXPO_PUBLIC_API_URL, which the harness sets.
 */
export default { expoConfig: null, expoGoConfig: null };
